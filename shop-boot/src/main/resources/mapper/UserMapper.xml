<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shop.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.shop.entity.User">
        <id column="uid" jdbcType="INTEGER" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="realname" jdbcType="VARCHAR" property="realName"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="addr" jdbcType="VARCHAR" property="address"/>
    </resultMap>

    <sql id="Base_Column_List">
        uid, username, password, realname, email, phone, addr
    </sql>

    <!-- 根据主键查询用户 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM zk_user
        WHERE uid = #{userId,jdbcType=INTEGER}
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="checkUsernameExists" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM zk_user
        WHERE username = #{username,jdbcType=VARCHAR}
    </select>

    <!-- 根据用户名和密码查询用户(登录) -->
    <select id="findByUsernameAndPassword" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM zk_user
        WHERE username = #{username,jdbcType=VARCHAR}
          AND password = #{password,jdbcType=VARCHAR}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM zk_user
        WHERE username = #{username,jdbcType=VARCHAR}
    </select>

    <!-- 根据主键删除用户 -->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        DELETE
        FROM zk_user
        WHERE uid = #{userId,jdbcType=INTEGER}
    </delete>

    <!-- 插入用户(全字段) -->
    <insert id="insert" parameterType="com.example.shop.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO zk_user (uid, username, password, realname, email, phone, addr)
        VALUES (#{userId,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
                #{realName,jdbcType=VARCHAR}, #{email,jdbcType=VARCHAR}, #{phone,jdbcType=VARCHAR},
                #{address,jdbcType=VARCHAR})
    </insert>

    <!-- 插入用户(选择性) -->
    <insert id="insertSelective" parameterType="com.example.shop.entity.User" useGeneratedKeys="true"
            keyProperty="userId">
        INSERT INTO zk_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">uid,</if>
            <if test="username != null">username,</if>
            <if test="password != null">password,</if>
            <if test="realName != null">realname,</if>
            <if test="email != null">email,</if>
            <if test="phone != null">phone,</if>
            <if test="address != null">addr,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId,jdbcType=INTEGER},</if>
            <if test="username != null">#{username,jdbcType=VARCHAR},</if>
            <if test="password != null">#{password,jdbcType=VARCHAR},</if>
            <if test="realName != null">#{realName,jdbcType=VARCHAR},</if>
            <if test="email != null">#{email,jdbcType=VARCHAR},</if>
            <if test="phone != null">#{phone,jdbcType=VARCHAR},</if>
            <if test="address != null">#{address,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <!-- 根据主键更新用户(选择性) -->
    <update id="updateByPrimaryKeySelective" parameterType="com.example.shop.entity.User">
        UPDATE zk_user
        <set>
            <if test="username != null">username = #{username,jdbcType=VARCHAR},</if>
            <if test="password != null">password = #{password,jdbcType=VARCHAR},</if>
            <if test="realName != null">realname = #{realName,jdbcType=VARCHAR},</if>
            <if test="email != null">email = #{email,jdbcType=VARCHAR},</if>
            <if test="phone != null">phone = #{phone,jdbcType=VARCHAR},</if>
            <if test="address != null">addr = #{address,jdbcType=VARCHAR},</if>
        </set>
        WHERE uid = #{userId,jdbcType=INTEGER}
    </update>

    <!-- 根据主键更新用户(全字段) -->
    <update id="updateByPrimaryKey" parameterType="com.example.shop.entity.User">
        UPDATE zk_user
        SET username = #{username,jdbcType=VARCHAR},
            password = #{password,jdbcType=VARCHAR},
            realname = #{realName,jdbcType=VARCHAR},
            email = #{email,jdbcType=VARCHAR},
            phone = #{phone,jdbcType=VARCHAR},
            addr = #{address,jdbcType=VARCHAR}
        WHERE uid = #{userId,jdbcType=INTEGER}
    </update>

</mapper>
